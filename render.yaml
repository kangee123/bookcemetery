services:
  - type: web
    name: bookcemetery
    env: python
    plan: free
    buildCommand: "pip install -r requirements.txt"
    startCommand: "python3 server.py"
    healthCheckPath: /opening-screen-stage1.html
    envVars:
      - key: KAKAO_REST_API_KEY
        sync: false
